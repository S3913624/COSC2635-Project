var myDecimal = 2.0;
var multiDecimal = 2.5;
var product = myDecimal * multiDecimal;
console.log(product);

var newDecimal = myDecimal / multiDecimal;
console.log(newDecimal);

/* Finding the remainder of a division function 
Useful for determining if a number is odd or even.
If a variable can be divided by two with no remainder,
the number is even. */
var remainder;
remainder = 11 % 2;
console.log(remainder);